services:
  - type: web
    name: irp-recarga-bits  # Substitua por um nome de serviço relevante
    env: php
    plan: free
    buildCommand: ""
    startCommand: "php -S 0.0.0.0:$PORT -t C:/xampp/htdocs/abc"
